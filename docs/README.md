# Math formulas
## Area
- Circle: S = πR²
- Rectangle: S = ab
- Square: S = a²

## Perimeter
- Circle: P = 2πR
- Rectangle: P = 2a + 2b
- Square: P = 4a

# Общее описание решения
## В проекте представлены формулы и показано как они работают на конкретных примерах. Список формул:
### формулы нахождения площади:
- круга
- квадрата
- прямоугольника
- треугольника
### формулы нахождения периметра:
- круга
- квадрата
- прямоугольника
- треугольника



## Описание работы файла circle.py:
### Описание работы функции area(r):
- Принимает число int(r)
- Выводит площадь круга с радиусом `r` (пи * r^2)
### *Пример вызова функции*
- `area(13)` => 530.929158456675

### Описание работы функции perimeter(r):
- Принимает число int(r)
- Выводит длину окружности с радиусом `r` (2 * пи * r)
### *Пример вызова функции*
- `perimeter(13)` => 81.68140899333463




## Описание работы файла square.py:
### Описание работы функции area(a):
- Принимает число int(a)
- Возвращает площадь квадрата со стороной `a` (a * a)
### *Пример вызова функции*
- `area(13)` => 169

### Описание работы функции perimeter(a):
- Принимает число int(a)
- Возвращает периметр квадрата со стороной `a` (a * 4)
### *Пример вызова функции*
- `perimeter(13)` => 52





## Описание работы файла triangle.py:
### Описание работы функции area(a, h):
- Принимает числa int(a, h)
- Возваращает площадь треугольника со стороной `a` и высотой `h` (a * h * 0.5)
### *Пример вызова функции*
- `area(13, 4)` => 26

### Описание работы функции perimeter(a, b, c):
- Принимает числа int(a, b, c)
- Возваращает периметр треугольника со сторонами `a`, `b`, `c` (a + b + c) 

### *Пример вызова функции*
- `perimeter(13, 13, 13)` => 39



## Описание работы файла rectangle.py:
### Описание работы функции area(a, b):
- Принимает числa int(a, b)
- Возваращает площадь прямоугольника со сторонами `a` , `b` (a * b)
### *Пример вызова функции*
- `area(13, 3)` => 39

### Описание работы функции perimeter(a, b):
- Принимает числа int(a, b)
- Возваращает периметр прямоугольника со сторонами `a`, `b` ((а + b) * 2)

### *Пример вызова функции*
- `perimeter(13, 13)` => 39


# Описание работы Unit-тестов

## Unit-тесты для файла *circle.py*:
### Проверка функции по поиску площади:
- Unit-тест `test_area_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_area_2()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
- Unit-тест `test_area_3()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_area_4()` проверяет корректность поведения программы при входных данных типа `string` (успешно)
- Unit-тест `test_area_5()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)
### Проверка функции по поиску периметра:
- Unit-тест `test_perimetr_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_perimetr_2()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
- Unit-тест `test_perimetr_3()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_perimetr_4()` проверяет корректность поведения программы при входных данных типа `string` (успешно)
- Unit-тест `test_perimetr_5()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)

## Тест-кейсы unittest для файла circle.py

| Номер теста      | Входные данные |    Ожидаемый результат     |   Фактический результат    | Статус  |
|------------------|:--------------:|:--------------------------:|:--------------------------:|---------|        
| 1) area(r)       |     r = 13     |      530.929158456675      |     530.929158456675       | Успешно | 
| 2) area(r)       |     r = -13    |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 3) area(r)       |     r = 0      |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 4) area(r)       |    r = 'mw'    |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 5) area(r)       |    r = True    |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 6) perimeter(r)  |     r = 13     |     81.68140899333463      |     81.68140899333463      | Успешно |
| 7) perimeter(r)  |     r = -13    |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 8) perimeter(r)  |     r = 0      |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 9) perimeter(r)  |    r = 'mw'    |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 10) perimeter(r) |    r = True    |     'Incorrect input'      |     'Incorrect input'      | Успешно |

## Unit-тесты для файла *square.py*:
### Проверка функции по поиску площади:
- Unit-тест `test_area_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_area_2()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_area_3()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
- Unit-тест `test_area_4()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)
- Unit-тест `test_area_5()` проверяет корректность поведения программы при входных данных типа `string` (успешно)
### Проверка функции по поиску периметра:
- Unit-тест `test_perimetr_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_perimetr_2()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_perimetr_3()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
- Unit-тест `test_perimetr_4()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)
- Unit-тест `test_perimetr_5()` проверяет корректность поведения программы при входных данных типа `string` (успешно)

## Тест-кейсы unittest для файла square.py

| Номер теста      | Входные данные |    Ожидаемый результат     |   Фактический результат    | Статус  |
|------------------|:--------------:|:--------------------------:|:--------------------------:|---------|        
| 1) area(r)       |     r = 3      |             9              |             9              | Успешно | 
| 2) area(r)       |     r = 0      |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 3) area(r)       |     r = -3     |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 4) area(r)       |    r = True    |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 5) area(r)       |     r = mw     |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 6) perimeter(r)  |     r = 3      |            12              |             12             | Успешно |
| 7) perimeter(r)  |     r = 0      |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 8) perimeter(r)  |     r = -3     |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 9) perimeter(r)  |    r = True    |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 10) perimeter(r) |    r = 'mw'    |     'Incorrect input'      |     'Incorrect input'      | Успешно |


## Unit-тесты для файла *triange.py*:
### Проверка функции по поиску площади:
- Unit-тест `test_area_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_area_2()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_area_3()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)
- Unit-тест `test_area_4()` проверяет корректность поведения программы при входных данных типа `string` (успешно)
- Unit-тест `test_area_5()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
### Проверка функции по поиску периметра:
- Unit-тест `test_perimetr_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_perimetr_2()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_perimetr_3()` проверяет корректность поведения программы при входных данных типа `string` (успешно)
- Unit-тест `test_perimetr_4()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
- Unit-тест `test_perimetr_5()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)

## Тест-кейсы unittest для файла triange.py

| Номер теста      | Входные данные       |    Ожидаемый результат     |   Фактический результат    | Статус  |
|------------------|:--------------------:|:--------------------------:|:--------------------------:|---------|        
| 1) area(r)       |  a = 3, b = 4        |             6              |             6              | Успешно | 
| 2) area(r)       |  a = 0, b = 3        |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 3) area(r)       | a = True, b = 3      |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 4) area(r)       | a = ms, b = 4        |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 5) area(r)       |  a =-3, b = 4        |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 6) perimeter(r)  |a = 4,b = 4,c = 4     |            12              |             12             | Успешно |
| 7) perimeter(r)  |a = 0,b = 4,c = 4     |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 8) perimeter(r)  |a = ms,b = 9,c = 7    |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 9) perimeter(r)  |a = -3,b = 4,c = 4    |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 10) perimeter(r) |a = True,b = 4,c = 4  |     'Incorrect input'      |     'Incorrect input'      | Успешно |


## Unit-тесты для файла *rectangle.py*:
### Проверка функции по поиску площади:
- Unit-тест `test_area_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_area_2()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_area_3()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
- Unit-тест `test_area_4()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)
- Unit-тест `test_area_5()` проверяет корректность поведения программы при входных данных типа `string` (успешно)
### Проверка функции по поиску периметра:
- Unit-тест `test_perimetr_1()` проверяет корректность работы программы с *валидными* входными данными (успешно)
- Unit-тест `test_perimetr_2()` проверяет корректность поведения программы при *нулевых* входных данных (успешно)
- Unit-тест `test_perimetr_3()` проверяет корректность поведения программы при *отрицательных* входных данных (успешно)
- Unit-тест `test_perimetr_4()` проверяет корректность поведения программы при входных данных типа `bool` (успешно)
- Unit-тест `test_perimetr_5()` проверяет корректность поведения программы при входных данных типа `string` (успешно)

## Тест-кейсы unittest для файла rectangle.py

| Номер теста      | Входные данные       |    Ожидаемый результат     |   Фактический результат    | Статус  |
|------------------|:--------------------:|:--------------------------:|:--------------------------:|---------|        
| 1) area(r)       |  a = 3, b = 3        |             9              |              9             | Успешно | 
| 2) area(r)       |  a = 10, b = 0       |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 3) area(r)       |  a = -3, b = -3      |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 4) area(r)       |  a = 2, b = True     |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 5) area(r)       |  a = 3, b = mv       |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 6) perimeter(r)  |  a = 3, b = 3        |            12              |            12              | Успешно |
| 7) perimeter(r)  |  a = 10, b = 0       |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 8) perimeter(r)  |  a = -3, b = -3      |      "Doesn't exist"       |      "Doesn't exist"       | Успешно |
| 9) perimeter(r)  |  a = 3, b = True     |     'Incorrect input'      |     'Incorrect input'      | Успешно |
| 10) perimeter(r) |  a = 3, b = mv       |     'Incorrect input'      |     'Incorrect input'      | Успешно |

# История изменения файлов.
![Хеши с коммитами](https://github.com/jff133/geometric_lib/blob/main/%D1%85.png?raw=true)
- 1debcd6 Add blocks with comments in cirle.py file 
- 8a9d50a Add blocks with comments in triangle.py file 
- 4425ff0 Add blocks with comments in rectangle.py file 
- 80af20d Add blocks with comments in square.py file 
- 

