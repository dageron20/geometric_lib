name: Run Tests

on:
  push:
    branches:  main 

jobs:
  test:
    name: Run tests.py
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run tests.py
      run: python tests.py
      shell: bash
